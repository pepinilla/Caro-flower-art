/* ======================================================
   CARO FLOWER ART – CONFIG
   ====================================================== */

window.CARO_CONFIG = {
  // Si true: oculta precios y muestra solo "Request quote"
  portfolioMode: false,

  // Si true: si el usuario NO ha elegido moneda antes,
  // intentamos decidir entre CAD/COP con heurística (idioma / timezone)
  autoCurrency: true,

  // Fallback si no se puede adivinar
  defaultCurrency: "CAD",

 
};